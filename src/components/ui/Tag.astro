---
interface Props { class?: string; }
const { class: className = '' } = Astro.props;
---
<span class={`inline-block bg-bg-alt text-muted text-[12px] font-semibold px-3 py-1 rounded-full border border-border ${className}`}>
  <slot />
</span>
